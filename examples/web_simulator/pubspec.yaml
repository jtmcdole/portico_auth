name: web_simulator
resolution: workspace
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  logging: ^1.2.0
  clock: ^1.1.1
  uuid: ^4.3.3
  google_fonts: ^8.0.0
  flutter_highlight: ^0.7.0
  json_annotation: ^4.9.0
  shelf: ^1.4.1
  web: any
  jose_plus: any

  # Internal packages (workspace members)
  portico_auth_client: any
  portico_auth_server_shelf: any
  portico_auth_credentials: any
  portico_auth_tokens: any
  portico_auth_roles: any
  crypto: ^3.0.7
  portico_auth_storage_yaml: ^1.0.0
  yaml: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  json_serializable: ^6.9.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
