name: portico_auth_roles
description: Role-Based Access Control (RBAC) metadata and assignment system.
version: 1.0.0
repository: https://github.com/jtmcdole/portico_auth/tree/main/packages/portico_auth_roles
issue_tracker: https://github.com/jtmcdole/portico_auth/issues

resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  equatable: ^2.0.7
  json_annotation: ^4.9.0
  # path: ^1.9.0

dev_dependencies:
  build_runner: ^2.10.4
  json_serializable: ^6.9.0
  lints: ^6.0.0
  test: ^1.25.0
