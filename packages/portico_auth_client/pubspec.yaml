name: portico_auth_client
resolution: workspace
description: A Dart-native SDK for user authentication and token management.
version: 1.0.0
repository: https://github.com/jtmcdole/portico_auth/tree/main/packages/portico_auth_client
issue_tracker: https://github.com/jtmcdole/portico_auth/issues

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  portico_auth_tokens: ^1.0.0
  clock: ^1.1.2
  collection: ^1.19.1
  http: ^1.3.0
  logging: ^1.3.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: any
